
# This is a random torch model generated by the following modules: ['ReLU6', 'CircularPad2d', 'BatchNorm2d']
import torch
import torch.nn as nn
import torch.nn.functional as F


class Model(nn.Module):
    def __init__(self) -> None:
        super().__init__()
        self.circular_pad1 = nn.CircularPad2d(2)
        self.batch_norm1 = nn.BatchNorm2d(3)
        self.relu6_1 = nn.ReLU6()
        self.circular_pad2 = nn.CircularPad2d(1)
        self.batch_norm2 = nn.BatchNorm2d(3)
        self.relu6_2 = nn.ReLU6()

    def forward(self, x):
        x = self.circular_pad1(x)
        x = self.batch_norm1(x)
        x = self.relu6_1(x)
        x = self.circular_pad2(x)
        x = self.batch_norm2(x)
        x = self.relu6_2(x)
        return x


def get_inputs():
    # randomly generate input tensors based on the model architecture
    x = torch.randn(1, 3, 32, 32).cuda()
    return [x]

def get_init_inputs():
    # randomly generate tensors required for initialization based on the model architecture
    return []
