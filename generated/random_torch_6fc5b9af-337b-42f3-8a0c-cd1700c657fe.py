
# This is a random torch model generated by the following modules: ['LazyBatchNorm3d', 'ReflectionPad3d', 'Softsign', 'CELU']
import torch
import torch.nn as nn
import torch.nn.functional as F


class Model(nn.Module):
    def __init__(self) -> None:
        super().__init__()
        self.bn1 = nn.LazyBatchNorm3d()
        self.pad1 = nn.ReflectionPad3d(1)
        self.bn2 = nn.LazyBatchNorm3d()
        self.pad2 = nn.ReflectionPad3d(2)
        self.bn3 = nn.LazyBatchNorm3d()
        self.softsign = nn.Softsign()
        self.celu = nn.CELU()

    def forward(self, x):
        x = self.bn1(x)
        x = self.pad1(x)
        x = self.bn2(x)
        x = self.pad2(x)
        x = self.bn3(x)
        x = self.softsign(x)
        x = self.celu(x)
        return x


def get_inputs():
    # randomly generate input tensors based on the model architecture
    x = torch.randn(1, 3, 32, 32, 32).cuda()  # Example input shape: (batch_size, channels, depth, height, width)
    return [x]

def get_init_inputs():
    # randomly generate tensors required for initialization based on the model architecture
    return []

