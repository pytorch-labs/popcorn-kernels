
# This is a random torch model generated by the following modules: ['LazyBatchNorm3d', 'ConstantPad3d', 'Dropout']
import torch
import torch.nn as nn


class Model(nn.Module):
    def __init__(self) -> None:
        super().__init__()
        self.bn1 = nn.LazyBatchNorm3d()
        self.pad1 = nn.ConstantPad3d(padding=1, value=0)
        self.dropout1 = nn.Dropout(p=0.5)
        self.bn2 = nn.LazyBatchNorm3d()
        self.pad2 = nn.ConstantPad3d(padding=2, value=0)
        self.dropout2 = nn.Dropout(p=0.3)
        self.bn3 = nn.LazyBatchNorm3d()

    def forward(self, x):
        x = self.bn1(x)
        x = self.pad1(x)
        x = self.dropout1(x)
        x = self.bn2(x)
        x = self.pad2(x)
        x = self.dropout2(x)
        x = self.bn3(x)
        return x


def get_inputs():
    # randomly generate input tensors based on the model architecture
    x = torch.randn(1, 3, 32, 32, 32).cuda()  # Example input shape: (batch_size, channels, depth, height, width)
    return [x]

def get_init_inputs():
    # randomly generate tensors required for initialization based on the model architecture
    return []
