
# This is a random torch model generated by the following modules: ['PixelShuffle', 'PoissonNLLLoss', 'Module', 'SmoothL1Loss', 'ReLU', 'AlphaDropout']
import torch
import torch.nn as nn
import torch.nn.functional as F


class Model(nn.Module):
    def __init__(self) -> None:
        super().__init__()
        self.pixel_shuffle = nn.PixelShuffle(2)
        self.relu = nn.ReLU()
        self.alpha_dropout = nn.AlphaDropout(p=0.5)
        self.poisson_nll_loss = nn.PoissonNLLLoss()
        self.smooth_l1_loss = nn.SmoothL1Loss()
        self.module = nn.Module()

    def forward(self, x):
        # Apply PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply ReLU
        x = self.relu(x)
        
        # Apply AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another AlphaDropout
        x = self.alpha_dropout(x)
        
        # Apply another ReLU
        x = self.relu(x)
        
        # Apply another PixelShuffle
        x = self.pixel_shuffle(x